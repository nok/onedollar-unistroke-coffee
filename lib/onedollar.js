// Generated by CoffeeScript 1.6.2
(function() {
  "use strict";  window.OneDollar = (function() {
    var Result, Vector;

    Vector = (function() {
      function Vector(x, y) {
        this.x = x != null ? x : 0.0;
        this.y = y != null ? y : 0.0;
      }

      Vector.prototype.dist = function(vector) {
        return Math.sqrt(Math.pow(this.x - vector.x, 2) + Math.pow(this.y - vector.y, 2));
      };

      Vector.prototype.add = function(value) {
        if (value instanceof Vector) {
          this.x += value.x;
          this.y += value.y;
        } else {
          this.x += value;
          this.y += value;
        }
        return this;
      };

      Vector.prototype.div = function(value) {
        if (value instanceof Vector) {
          this.x /= value.x;
          this.y /= value.y;
        } else {
          this.x /= value;
          this.y /= value;
        }
        return this;
      };

      Vector.prototype.mult = function(value) {
        if (value instanceof Vector) {
          this.x *= value.x;
          this.y *= value.y;
        } else {
          this.x *= value;
          this.y *= value;
        }
        return this;
      };

      return Vector;

    })();

    Result = (function() {
      function Result(name, percent) {
        this.name = name != null ? name : '';
        this.percent = percent != null ? percent : 0.0;
      }

      return Result;

    })();

    function OneDollar(fragmentation, size, angle, step) {
      this.fragmentation = fragmentation != null ? fragmentation : 64;
      this.size = size != null ? size : 250;
      this.angle = angle != null ? angle : 45;
      this.step = step != null ? step : 3;
      this.PI = Math.PI;
      this.HALFDIAGONAL = 0.5 * Math.sqrt(this.size * this.size + this.size * this.size);
      this.templates = {};
    }

    OneDollar.prototype.learn = function(name, points) {
      if (points.length > 0) {
        this.templates[name] = this._transform(points);
      }
      return this;
    };

    OneDollar.prototype.check = function(points) {
      var result;

      if (points.length > 0) {
        points = this._transform(points);
      } else {
        return;
      }
      result = this.__equality(points);
      return points;
    };

    OneDollar.prototype._transform = function(points) {
      points = this.__convertToVectors(points);
      points = this.__resample(points);
      points = this.__rotateToZero(points);
      points = this.__scaleToSquare(points);
      points = this.__translateToOrigin(points);
      return points;
    };

    OneDollar.prototype.__convertToVectors = function(points) {
      var point, result, _i, _len;

      result = [];
      for (_i = 0, _len = points.length; _i < _len; _i++) {
        point = points[_i];
        result.push(new Vector(point[0], point[1]));
      }
      return result;
    };

    OneDollar.prototype.__resample = function(points) {
      var distance, i, point, prev, result, seperator, space, vector, _i, _ref, _ref1;

      seperator = (this.___distance(points)) / (this.fragmentation - 1);
      distance = 0;
      result = [];
      while (points.length !== 0) {
        prev = points.pop();
        if (result.length === 0) {
          result.push(prev);
        } else {
          if (points.length === 0) {
            result.push(prev);
            break;
          }
          point = points[points.length - 1];
          space = prev.dist(point);
          if ((distance + space) >= seperator) {
            vector = new Vector(prev.x + ((seperator - distance) / space) * (point.x - prev.x), prev.y + ((seperator - distance) / space) * (point.y - prev.y));
            result.push(vector);
            points.push(vector);
            distance = 0;
            if (result.length === (this.fragmentation - 1)) {
              result.push(points[points.length - 1]);
              break;
            }
          } else {
            distance += space;
          }
        }
      }
      if (result.length !== this.fragmentation) {
        point = result[result.length - 1];
        for (i = _i = _ref = result.length, _ref1 = this.fragmentation; _ref <= _ref1 ? _i < _ref1 : _i > _ref1; i = _ref <= _ref1 ? ++_i : --_i) {
          result.push(point);
        }
      }
      return result;
    };

    OneDollar.prototype.__rotateToZero = function(points) {
      var centroid, theta;

      centroid = this.___centroid(points);
      theta = Math.atan2(centroid.y - points[0].y, centroid.x - points[0].x);
      return this.___rotate(points, -theta, centroid);
    };

    OneDollar.prototype.__scaleToSquare = function(points) {
      var maxX, maxY, minX, minY, point, _i, _len;

      maxX = maxY = -Infinity;
      minX = minY = +Infinity;
      for (_i = 0, _len = points.length; _i < _len; _i++) {
        point = points[_i];
        minX = Math.min(point.x, minX);
        maxX = Math.max(point.x, maxX);
        minY = Math.min(point.y, minY);
        maxY = Math.max(point.y, maxY);
      }
      return this.___scale(points, new Vector(this.size / (maxX - minX), this.size / (maxY - minY)));
    };

    OneDollar.prototype.__translateToOrigin = function(points) {
      var centroid;

      centroid = this.___centroid(points);
      return this.___translate(points, centroid.mult(-1));
    };

    OneDollar.prototype.__equality = function(points) {
      var name, point, template, _i, _len, _ref;

      _ref = this.templates;
      for (name in _ref) {
        template = _ref[name];
        for (_i = 0, _len = template.length; _i < _len; _i++) {
          point = template[_i];
          console.log(point);
        }
      }
      return points;
    };

    OneDollar.prototype.___centroid = function(points) {
      var centroid, p, _i, _len;

      centroid = new Vector;
      for (_i = 0, _len = points.length; _i < _len; _i++) {
        p = points[_i];
        centroid.add(p);
      }
      centroid.div(points.length);
      return centroid;
    };

    OneDollar.prototype.___distance = function(points) {
      var length, p, tmp, _i, _len;

      length = 0.0;
      tmp = null;
      for (_i = 0, _len = points.length; _i < _len; _i++) {
        p = points[_i];
        if (tmp !== null) {
          length += p.dist(tmp);
        }
        tmp = p;
      }
      return length;
    };

    OneDollar.prototype.___difference = function(template, candidate) {
      var distance, i, point, _i, _len;

      distance = 0.0;
      for (i = _i = 0, _len = template.length; _i < _len; i = ++_i) {
        point = template[i];
        distance += point.dist(candidate[i]);
      }
      return distance / template.length;
    };

    OneDollar.prototype.___translate = function(points, offset) {
      var point, _i, _len;

      for (_i = 0, _len = points.length; _i < _len; _i++) {
        point = points[_i];
        point.add(offset);
      }
      return points;
    };

    OneDollar.prototype.___scale = function(points, offset) {
      var point, _i, _len;

      for (_i = 0, _len = points.length; _i < _len; _i++) {
        point = points[_i];
        point.mult(offset.x);
      }
      return points;
    };

    OneDollar.prototype.___rotate = function(points, radians, pivot) {
      var cos, i, point, sin, x, y, _i, _len;

      sin = Math.sin(radians);
      cos = Math.cos(radians);
      for (i = _i = 0, _len = points.length; _i < _len; i = ++_i) {
        point = points[i];
        x = (point.x - pivot.x) * cos - (point.y - pivot.y) * sin + pivot.x;
        y = (point.x - pivot.x) * sin + (point.y - pivot.y) * cos + pivot.y;
        points[i] = new Vector(x, y);
      }
      return points;
    };

    OneDollar.prototype.___radians = function(degrees) {
      return degrees * Math.PI / 180.0;
    };

    return OneDollar;

  })();

}).call(this);
